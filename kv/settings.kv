<SettingsScreen>
    BoxLayout:
        orientation: 'vertical'
        canvas.before:
            Color:
                rgba: get_color_from_hex(Colors.BACKGROUND)

            Rectangle:
                pos: self.pos
                size: self.size

        BoxLayout:
            orientation: 'horizontal'
            size_hint_y: None
            height: '64dp'
            padding: '16dp', 0
            spacing: '8dp'
            canvas.before:
                Color:
                    rgba: get_color_from_hex(Colors.BRAND_PRIMARY)

                Rectangle:
                    pos: self.pos
                    size: self.size
                    
            MDIconButton:
                icon: 'arrow-left'
                on_release: root.go_to_dashboard()
                theme_icon_color: 'Custom'
                icon_color: get_color_from_hex(Colors.TEXT_ON_PRIMARY)
                pos_hint: {'center_y': .5}

            MDLabel:
                text: 'Settings'
                font_size: '26sp'
                size_hint_x: 1
                theme_text_color: 'Custom'
                text_color: get_color_from_hex(Colors.TEXT_ON_PRIMARY)

        Widget:
            size_hint_y: None
            height: "1dp"

            canvas.before:
                Color:
                    rgba: get_color_from_hex(Colors.BORDER)

                Rectangle:
                    pos: self.pos
                    size: self.size        

        ScrollView:
            size_hint_y: 1
            do_scroll_x: False

            BoxLayout:
                orientation: "vertical"
                size_hint_y: None
                height: self.minimum_height
                padding: "16dp"
                spacing: "12dp"

                MDLabel:
                    text: "About"
                    size_hint_y: None
                    height: self.texture_size[1]
                    text_size: self.width, None
                    font_size: "18sp"
                    bold: True
                    theme_text_color: "Custom"
                    text_color: get_color_from_hex(Colors.TEXT_PRIMARY)

                OutlinedCard:
                    orientation: "vertical"
                    size_hint_y: None
                    height: self.minimum_height
                    padding: "16dp"
                    spacing: "8dp"
                    md_bg_color: get_color_from_hex(Colors.SURFACE)

                    MDLabel:
                        text: "Enkryon"
                        size_hint_y: None
                        height: self.texture_size[1]
                        text_size: self.width, None
                        font_size: "16sp"
                        bold: True
                        theme_text_color: "Custom"
                        text_color: get_color_from_hex(Colors.BRAND_PRIMARY)

                    MDLabel:
                        text: "Version {}".format(app.version)
                        size_hint_y: None
                        height: self.texture_size[1]
                        text_size: self.width, None
                        font_size: "13sp"
                        theme_text_color: "Custom"
                        text_color: get_color_from_hex(Colors.TEXT_SECONDARY)

                    MDLabel:
                        text: "Simple, local-first personal finance tracking."
                        size_hint_y: None
                        height: self.texture_size[1]
                        text_size: self.width, None
                        font_size: "14sp"
                        theme_text_color: "Custom"
                        text_color: get_color_from_hex(Colors.TEXT_PRIMARY)

                MDLabel:
                    text: "Data & Privacy"
                    size_hint_y: None
                    height: self.texture_size[1]
                    text_size: self.width, None
                    font_size: "18sp"
                    bold: True
                    theme_text_color: "Custom"
                    text_color: get_color_from_hex(Colors.TEXT_PRIMARY)

                OutlinedCard:
                    orientation: "vertical"
                    size_hint_y: None
                    height: self.minimum_height
                    padding: "16dp"
                    spacing: "8dp"
                    md_bg_color: get_color_from_hex(Colors.SURFACE)

                    MDLabel:
                        text: "Local data and privacy"
                        size_hint_y: None
                        height: self.texture_size[1]
                        text_size: self.width, None
                        font_size: "16sp"
                        bold: True
                        theme_text_color: "Custom"
                        text_color: get_color_from_hex(Colors.BRAND_PRIMARY)

                    MDLabel:
                        text: "Your accounts, categories, and transactions are stored locally on this device. Enkryon does not require an account or upload your financial data."
                        size_hint_y: None
                        height: self.texture_size[1]
                        text_size: self.width, None
                        font_size: "14sp"
                        theme_text_color: "Custom"
                        text_color: get_color_from_hex(Colors.TEXT_PRIMARY)

                    MDLabel:
                        text: "Android automatic backup is currently disabled. Uninstalling Enkryon, clearing its app data, or choosing Clear All Data can permanently remove your records. Backup and restore are not available yet."
                        size_hint_y: None
                        height: self.texture_size[1]
                        text_size: self.width, None
                        font_size: "13sp"
                        theme_text_color: "Custom"
                        text_color: get_color_from_hex(Colors.TEXT_SECONDARY)

                OutlinedCard:
                    orientation: "vertical"
                    size_hint_y: None
                    height: self.minimum_height
                    padding: "16dp"
                    spacing: "4dp"
                    ripple_behavior: True
                    md_bg_color: get_color_from_hex(Colors.SURFACE)
                    on_release: root.clear_data()

                    MDLabel:
                        text: "Clear All Data"
                        size_hint_y: None
                        height: self.texture_size[1]
                        text_size: self.width, None
                        theme_text_color: "Error"
                        bold: True

                    MDLabel:
                        text: "Permanently delete every account, category, and transaction stored by Enkryon."
                        size_hint_y: None
                        height: self.texture_size[1]
                        text_size: self.width, None
                        font_size: "13sp"
                        theme_text_color: "Custom"
                        text_color: get_color_from_hex(Colors.TEXT_SECONDARY)
